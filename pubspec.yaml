name: flutter_game_performance_utils
description: Performance utilities for Flutter mobile games. Debouncing, throttling, asset caching, and Firebase connection management.
version: 0.1.0
homepage: https://github.com/yourusername/flutter_game_performance_utils

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
